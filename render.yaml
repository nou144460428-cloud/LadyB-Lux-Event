services:
  - type: web
    name: ladyb-lux-event-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
